<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bumian.memorynotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.bumian.memorynotes.NotesApplication"
        android:theme="@style/Theme.MemoryNotes">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="_._"/> <!-- paste api key here before running app -->

        <activity
            android:name="com.bumian.memorynotes.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>